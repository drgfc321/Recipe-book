# Application
quarkus.application.name=dnd-backend
quarkus.http.port=8080

# Database Configuration
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${POSTGRES_USER:dnd_user}
quarkus.datasource.password=${POSTGRES_PASSWORD:dnd_secret}
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:${POSTGRES_PORT:5432}/${POSTGRES_DB:dnd_campaign}

# Hibernate ORM
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true

# CORS - Allow frontend to call API
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:8081,http://127.0.0.1:8081
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=location,info
quarkus.http.cors.access-control-max-age=24H
quarkus.http.cors.access-control-allow-credentials=true

# JWT Configuration
mp.jwt.verify.issuer=dnd-campaign-manager
mp.jwt.verify.publickey.location=publicKey.pem
smallrye.jwt.sign.key.location=privateKey.pem
smallrye.jwt.new-token.lifespan=86400
jwt.duration.hours=24

# Security - permit auth endpoints without token
quarkus.http.auth.permission.public.paths=/api/auth/register,/api/auth/login,/hello
quarkus.http.auth.permission.public.policy=permit

# Require authentication for all other /api/* endpoints
quarkus.http.auth.permission.authenticated.paths=/api/*
quarkus.http.auth.permission.authenticated.policy=authenticated

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.dnd".level=DEBUG
